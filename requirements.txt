# --- Cérebro ---------------------------------------------------------------
anthropic>=0.116.0

# --- Áudio: captura e reprodução -------------------------------------------
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.26

# --- Ouvidos: transcrição local (offline, o áudio não sai da máquina) ------
faster-whisper>=1.0.3

# --- Boca: síntese de voz ---------------------------------------------------
# piper-tts é o padrão (offline, rápido em CPU, vozes pt-BR).
# edge-tts precisa de internet mas soa melhor; pyttsx3 é o último recurso.
piper-tts>=1.2.0
edge-tts>=6.1.9
pyttsx3>=2.90

# --- Mãos: controle do Windows ---------------------------------------------
pyautogui>=0.9.54
pyperclip>=1.8.2
pygetwindow>=0.0.9
psutil>=5.9.0
pillow>=10.0.0
send2trash>=1.8.2

# --- Só no Windows ----------------------------------------------------------
pywin32>=306; sys_platform == "win32"
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2.0; sys_platform == "win32"
